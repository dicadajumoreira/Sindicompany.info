# Engine de PDF
weasyprint>=63.0
Pillow>=10.0

# Supabase (DB + Storage) — usado pelo api/generate.py
supabase>=2.5.0

# Geração de texto (cartas personalizadas)
openai>=1.50.0

# Drive integration (frente futura)
gdown>=5.0.0
google-api-python-client>=2.100.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0

# Auxiliares
markdown>=3.5
pyyaml>=6.0
